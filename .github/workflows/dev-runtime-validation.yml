name: Dev runtime validation

on:
  push:
    branches: [ci/mobux-mobile-input-dev-runtime-20260816]

jobs:
  dev-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-directories: ~/.cache/sherpa-rs
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: sudo apt-get update && sudo apt-get install -y lsof
      - name: Start dev backend and SPA
        run: make dev-up
        env:
          MOBUX_USER: dev
          MOBUX_PIN: '00000'
          MOBUX_DATA_DIR: ${{ runner.temp }}/mobux-dev-data
          DEV_LOG_DIR: ${{ runner.temp }}/mobux-dev-logs
      - name: Verify dev listeners and HTTP
        shell: bash
        env:
          DEV_LOG_DIR: ${{ runner.temp }}/mobux-dev-logs
        run: |
          set -euo pipefail
          lsof -nP -iTCP:5152 -sTCP:LISTEN
          lsof -nP -iTCP:5173 -sTCP:LISTEN
          if curl --fail --silent --show-error --user dev:00000 http://127.0.0.1:5152/ >/dev/null 2>&1; then
            backend_url='http://127.0.0.1:5152/'
          elif curl --fail --silent --show-error --insecure --user dev:00000 https://127.0.0.1:5152/ >/dev/null 2>&1; then
            backend_url='https://127.0.0.1:5152/'
          else
            echo 'backend listener did not answer HTTP or HTTPS' >&2
            cat "$DEV_LOG_DIR/backend.log" >&2 || true
            exit 1
          fi
          curl --fail --silent --show-error http://127.0.0.1:5173/static/spa/ >/dev/null
          echo "DEV_RUNTIME backend=${backend_url} spa=http://127.0.0.1:5173/static/spa/"
      - name: Stop dev backend and SPA
        if: always()
        run: |
          make dev-down
          ! lsof -i :5152 >/dev/null 2>&1
          ! lsof -i :5173 >/dev/null 2>&1
